name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:

          - service: accounting
            dockerfile: Dockerfile
            context: ./src/accounting

          - service: ad
            dockerfile: Dockerfile
            context: ./src/ad

          - service: cart
            dockerfile: src/Dockerfile
            context: ./src/cart

          - service: checkout
            dockerfile: Dockerfile
            context: ./src/checkout

          - service: currency
            dockerfile: Dockerfile
            context: ./src/currency
            build_args: --build-arg OPENTELEMETRY_CPP_VERSION=1.14.2

          - service: email
            dockerfile: Dockerfile
            context: ./src/email

          - service: flagd-ui
            dockerfile: Dockerfile
            context: ./src/flagd-ui

          - service: fraud-detection
            dockerfile: Dockerfile
            context: ./src/fraud-detection
            build_args: --build-arg OTEL_JAVA_AGENT_VERSION=2.9.0

          - service: frontend
            dockerfile: Dockerfile
            context: ./src/frontend

          - service: frontend-proxy
            dockerfile: Dockerfile
            context: ./src/frontend-proxy

          - service: image-provider
            dockerfile: Dockerfile
            context: ./src/image-provider

          - service: kafka
            dockerfile: Dockerfile
            context: ./src/kafka
            build_args: --build-arg OTEL_JAVA_AGENT_VERSION=2.9.0

          - service: load-generator
            dockerfile: Dockerfile
            context: ./src/load-generator

          - service: opensearch
            dockerfile: Dockerfile
            context: ./src/opensearch

          - service: payment
            dockerfile: Dockerfile
            context: ./src/payment

          - service: postgres
            dockerfile: Dockerfile
            context: ./src/postgres

          - service: product-catalog
            dockerfile: Dockerfile
            context: ./src/product-catalog

          - service: quote
            dockerfile: Dockerfile
            context: ./src/quote

          - service: recommendation
            dockerfile: Dockerfile
            context: ./src/recommendation

          - service: shipping
            dockerfile: Dockerfile
            context: ./src/shipping

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build Docker image
        run: |
          IMAGE=${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ matrix.service }}:${{ github.run_number }}

          docker build \
            -t $IMAGE \
            -f ${{ matrix.dockerfile }} \
            ${{ matrix.build_args }} \
            ${{ matrix.context }}

      - name: Push Docker image
        run: |
          IMAGE=${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ matrix.service }}:${{ github.run_number }}
          docker push $IMAGE